# Global
domain: kester.pro
storageClass: "do-block-storage"
logging:
  enable: true
observability:
  enable: true
  pdkey:
  storageSize: 50Gi
  retention: 5d
traefik:
  enable: true  
trivy:
  enable: true
